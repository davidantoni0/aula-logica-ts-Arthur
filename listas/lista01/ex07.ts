const nota01: number = Number(prompt('Digite o valor da primeira nota'));
const nota02: number = Number(prompt('Digite o valor da segunda nota'));

const media: number = (nota01 + nota02) / 2;

alert(`A média entre ${nota01} e ${nota02} é ${media}`)