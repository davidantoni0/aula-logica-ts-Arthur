const base: number = Number(prompt('Digite o valor em metros da base do retângulo'))
const altura: number = Number(prompt('Digite o valor em metros da altura do retângulo'))

const area: number = base * altura

alert(`a área do retangulo é de ${area}m².`)